name: state_management
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ">=3.6.1 <4.0.0"
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints:
